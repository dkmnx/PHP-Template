# Application
APP_NAME=PHP-Template
APP_ENV=development

# Database
DB_HOST=localhost
DB_NAME=your_database_name
DB_USER=your_database_user

# Database Password
# Note: Empty password is valid for local development with socket authentication
# or when using peer/ident authentication methods. Always set a strong password
# for production environments.
DB_PASS=your_database_password
